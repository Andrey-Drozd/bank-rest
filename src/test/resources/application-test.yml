spring:
  datasource:
    url: jdbc:h2:mem:bankcards-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  liquibase:
    enabled: true
    contexts: test

security:
  jwt:
    secret: test-jwt-secret-test-jwt-secret-123
  card-encryption:
    secret: test-card-encryption-secret
